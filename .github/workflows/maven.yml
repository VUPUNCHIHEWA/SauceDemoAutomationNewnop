name: QA Automation CI Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: maven

      - name: Run Automated Tests with Maven
        run: mvn test
        continue-on-error: true

      - name: Print Test Results to Console
        run: |
          echo "=================== TEST FAILURES SUMMARY ==================="
          if [ -f target/surefire-reports/testng-results.xml ]; then
            cat target/surefire-reports/testng-results.xml | grep -E "exception|message" || echo "No detailed XML exceptions found."
          fi
          echo "============================================================="

      - name: Upload Test Reports Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-execution-reports
          path: target/surefire-reports/